# MLflow Demo

## Project Overview
This project demonstrates how to use MLflow for tracking experiments and managing models.

## Setup Instructions
1. Clone the repository.
2. Install dependencies from `requirements.txt`.
3. Run the training script `mlflow_demo.py` to start logging metrics and models to MLflow.